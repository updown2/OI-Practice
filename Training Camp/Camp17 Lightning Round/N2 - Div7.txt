#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define For(i, a, b) for(ll i=a; i<b; i++)
#define ffi For(i, 1, N)
#define ffj For(j, 1, 7)
#define s <<" "<<
#define w cout
#define e endl
#define pb push_back
#define mp make_pair
#define a first
#define b second
//500,000,000 operations
//Global Variables
ll N;
ll nums[7][2];
ll a;
ll b=0;
ll out;
 
int main() {
    //ifstream cin ("test.in");
    cin >> N;
 
	ffj {
		nums[j][0] = -1;
		nums[j][1] = -1;
	}
 
	ffi {
		cin >> a;
		b += a;
		b = b%7;
		if (nums[b][0] == -1) {
			nums[b][0] = i;
		}
		nums[b][1] = i;
	}
 
	out = nums[0][1];
	ffj {
		out = max(out, nums[j][1]-nums[j][0]);
	}
 
    cout << out << endl;
}