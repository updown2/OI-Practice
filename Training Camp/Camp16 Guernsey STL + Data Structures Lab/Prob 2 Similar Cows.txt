#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define For(i, a, b) for(ll i=a; i<b; i++)
#define ffi For(i, 0, N)
#define ffj For(j, 0, M)
#define ffa ffi ffj
#define s <<" "<<
#define w cout
#define e endl
#define pb push_back
#define mp make_pair
#define a first
#define b second
#define int ll
const int MAXN = 100000;
//500,000,000 operations
int N;
multiset<string> have;
multiset<string>::iterator it1, it2;

main() {
	//ifstream cin ("test.in");
	ios_base::sync_with_stdio(0); cin.tie(0);
	cin >> N;
	ffi {
        string a, b, c; cin >> a >> b;
        if (a == "BUY") have.insert(b);
        if (a == "SELL") {
            cin >> c;
            it1 = have.lower_bound(b);
            it2 = have.upper_bound(c);
            have.erase(it1, it2);
        }
	}
	for (string i: have) w<<i<<e;
}